# Backend MongoDB CRUD API

A simple backend CRUD API for client

## Installation

In terminal, run the following command:

```bash
npm install
```

Create file .env

```env
PORT=3000
DB_MONGO=mongodb://localhost:27017/client
```
